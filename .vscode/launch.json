{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug openfsdecrypt (Windows)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/openfsdecrypt.exe",
            "args": [],
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "cargo build"
        }
    ]
}